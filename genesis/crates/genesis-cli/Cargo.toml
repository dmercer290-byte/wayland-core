[package]
name = "genesis-cli"
description = "Genesis — a provider-neutral AI agent for your terminal"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "genesis"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
genesis-engine = { path = "../genesis-engine" }
serde_json = { workspace = true }
tokio = { workspace = true }
